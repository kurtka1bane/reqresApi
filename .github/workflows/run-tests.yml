name: Run API Tests with Gradle

on:
  workflow_dispatch:  # Ручной запуск
  push:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Клонируем код
      - uses: actions/checkout@v4

      # Шаг 2: Устанавливаем Java
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Шаг 3: Делаем gradlew исполняемым
      - name: Make gradlew executable
        run: chmod +x gradlew

      # Шаг 4: Запускаем тесты
      - name: Run tests
        id: run_tests  # Добавляем ID для проверки статуса тестов
        run: ./gradlew test

      # Шаг 5: Сохраняем отчёты с ID
      - name: Upload test reports
        id: upload_artifacts  # Важно: добавляем ID для получения artifact-id
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-reports
          path: |
            build/reports/tests/
            build/test-results/

      # Небольшая пауза для обработки артефактов
      - name: Wait for artifacts processing
        if: always()
        run: sleep 10  # Пауза 10 секунд

      # Шаг 6: Отправка отчета в Telegram
      - name: Send test results to Telegram
        if: always()  # Отправлять даже если тесты упали
        uses: appleboy/telegram-action@master
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          message: |
            📊 *Результаты тестов Reqres.in*
            ├─ Статус: ${{ steps.run_tests.outcome == 'success' && '✅ Успешно' || '❌ Ошибка' }}
            ├─ Запуск: [${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})
            ├─ Ветка: ${{ github.ref_name }}
            ├─ Артефакты: [Скачать](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts/${{ steps.upload_artifacts.outputs.artifact-id }})
            └─ Альтернативная ссылка: [Все артефакты](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}?check_suite_focus=true#artifacts)
          parse_mode: markdown