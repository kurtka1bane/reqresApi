name: Run API Tests with Gradle

on:
  workflow_dispatch:  # Только ручной запуск

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # Шаг 1: Клонируем код
      - uses: actions/checkout@v4

      # Шаг 2: Устанавливаем Java
      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          java-version: '17'
          distribution: 'temurin'

      # Шаг 3: Делаем gradlew исполняемым
      - name: Make gradlew executable
        run: chmod +x gradlew

      # Шаг 4: Запускаем тесты с сохранением результатов
      - name: Run tests and parse results
        id: run_tests
        run: |
          # Запускаем все тесты (даже если часть упадёт)
          ./gradlew test --continue || true
          
          # Устанавливаем Python для парсинга
          sudo apt-get update && sudo apt-get install -y python3
          
          # Создаём Python-скрипт для парсинга результатов
          cat << 'EOF' > parse_tests.py
          import xml.etree.ElementTree as ET
          import glob
          import json
          failed_tests = []
          for xml_file in glob.glob("build/test-results/test/*.xml"):
              tree = ET.parse(xml_file)
              root = tree.getroot()
              for testcase in root.findall(".//testcase"):
                  if testcase.find("failure") is not None:
                      classname = testcase.get("classname").split(".")[-1]
                      name = testcase.get("name")
                      failed_tests.append(f"{classname}.{name}")
          # Выводим как JSON-массив (пустой массив, если тестов нет)
          print(f"::set-output name=failed::{json.dumps(failed_tests) if failed_tests else '[]'}")
          print(f"::set-output name=failed_count::{len(failed_tests)}")
          EOF
          
          # Выполняем парсинг
          python3 parse_tests.py >> $GITHUB_OUTPUT
          
          # Считаем общее количество тестов
          TOTAL_TESTS=$(grep -o 'tests="[0-9]*"' build/test-results/test/*.xml | awk -F'"' '{sum+=$2} END {print sum}')
          echo "::set-output name=total::$TOTAL_TESTS" >> $GITHUB_OUTPUT
          echo "::set-output name=passed::$(($TOTAL_TESTS - ${steps.run_tests.outputs.failed_count}))" >> $GITHUB_OUTPUT

      # Шаг 5: Сохраняем отчёты
      - name: Upload test reports
        id: upload_artifacts
        uses: actions/upload-artifact@v4
        if: always()
        with:
          name: test-reports
          path: |
            build/reports/tests/
            build/test-results/

      # Шаг 6: Отправка детального отчета в Telegram
      - name: Send detailed test results to Telegram
        if: always()
        uses: appleboy/telegram-action@v1.0.0
        with:
          token: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          to: ${{ secrets.TELEGRAM_CHAT_ID }}
          message: |
            "📊 *Детальные результаты тестов*"
            "├─ Всего тестов: ${{ steps.run_tests.outputs.total }}"
            "├─ ✅ Успешно: ${{ steps.run_tests.outputs.passed }}"
            "├─ ❌ Упало: ${{ steps.run_tests.outputs.failed_count }}"
            ${{ steps.run_tests.outputs.failed_count != '0' && format('├─ Список упавших:\n├───── {0}', join(fromJSON(steps.run_tests.outputs.failed), '\n├───── ')) || '├─ Все тесты прошли успешно!' }}
            "├─ Запуск: [${{ github.run_number }}](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }})"
            "└─ Отчёт: [Скачать](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}/artifacts/${{ steps.upload_artifacts.outputs.artifact-id }})"
          format: "markdown"
          disable_web_page_preview: true